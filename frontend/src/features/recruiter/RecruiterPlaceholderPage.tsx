export function RecruiterPlaceholderPage() {
  return (
    <section>
      <h1>Recruiter Portal</h1>
      <p>Login and the recruiter workspace (sourcing, screening, jobs, reports) land in Phase 2+.</p>
    </section>
  );
}
